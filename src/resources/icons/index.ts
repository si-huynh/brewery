import { createIconSetFromFontello } from 'react-native-vector-icons'

import config from './config.json'

const Icon = createIconSetFromFontello(config, 'fontello', 'fontello.ttf')

export default Icon
